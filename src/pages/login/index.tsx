import { Background, Button, Input } from "../../components";

export default function Login() {
  return (
    <Background>
      <Button scheme="green" />
      <Input placeholder="Email" />
    </Background>
  );
}
